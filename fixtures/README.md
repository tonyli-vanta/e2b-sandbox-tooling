# Fixtures

Deterministic test files generated by [`../gen_fixtures.py`](../gen_fixtures.py) and exercised by
[`../e2b-run-fixtures.cjs`](../e2b-run-fixtures.cjs). Each has known content so expected extraction
output is predictable. Re-run the generator to reproduce these byte-for-byte.

| File | Capability-matrix row | Command to test | Expected result |
|---|---|---|---|
| `sample.csv` | CSV | `pandas.read_csv` | ✅ 3 rows (Alpha/Beta/Gamma) |
| `sample.html` | HTML/XML | `bs4` + `lxml` | ✅ text extracted |
| `sample.xlsx` | XLSX | `pandas.read_excel` / `openpyxl` | ✅ data read; **gotchas baked in**: `openpyxl max_row=500` (phantom — only 3 data rows) and a `mailto:` hyperlink in `C2` that `pandas` drops but `openpyxl cell.hyperlink.target` recovers |
| `sample_legacy.xls` | Legacy XLS | `pandas` + `xlrd` | ✅ read |
| `text.pdf` | Text PDF | `pdftotext` / `pypdf` | ✅ real text layer ("COMPLIANCE", "12345") |
| `scanned.pdf` | Scanned/image PDF | `pdftotext` | ❌ ~0 chars — **OCR gap** (text is pixels; no `tesseract`). `pdftoppm` rasterizes fine. |
| `image_text.png` | Images (text in) | `Pillow` | ✅ opens (800×200); ❌ no OCR for the text |
| `sample.docx` | Word .docx | `python-docx` | ✅ all paragraphs ("AUDIT", "42") |
| `sample.pptx` | PowerPoint .pptx | `import pptx` | ❌ `ModuleNotFoundError` — **gap** (no `python-pptx` in sandbox) |

Not included: a legacy binary `.doc` (Word 97–2003) — no tool to generate it locally or in-sandbox;
it is a gap row anyway. Download a public sample if a real `.doc` fixture is needed.
